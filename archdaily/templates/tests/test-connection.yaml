apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "archdaily.fullname" . }}-test-connection"
  labels:
    {{- include "archdaily.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "archdaily.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
