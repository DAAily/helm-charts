apiVersion: v1
kind: Service
metadata:
  name: {{ include "daaily.fullname" . }}
  labels:
    {{- include "daaily.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "daaily.selectorLabels" . | nindent 4 }}
