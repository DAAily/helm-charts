apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "daaily.fullname" . }}-test-connection"
  labels:
    {{- include "daaily.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "daaily.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
